version: 2.1
jobs:
  build-and-test:
    docker:
      - image: circleci/openjdk:8
    steps:
      - checkout
      - run: mvn clean package
      - run: mvn test
workflows:
  build-and-test-workflow:
    jobs:
      - build-and-test
    filters:
      branches:
        only:
          - master